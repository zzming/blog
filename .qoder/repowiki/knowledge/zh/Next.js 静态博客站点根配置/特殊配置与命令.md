`npm run dev` 启动开发服务器，`npm run build` 执行 `next build` 输出到 `out/` 静态目录，`npm run start` 以静态服务器运行 `out/`，`npm run lint` 调用 `next lint`，`npm run generate-posts` 执行 `node scripts/generate-posts.js` 预生成博文数据。