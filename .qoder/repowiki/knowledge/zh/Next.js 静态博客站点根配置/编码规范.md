- 所有源码通过 `@/*` 路径别名引用，指向 `./src/*`，避免相对路径嵌套。
- Tailwind 自定义扩展集中在 `tailwind.config.js` 的 `theme.extend` 中，并按元素族（h1-h4、ul/ol/li、table/tr/thead/tbody 等）逐一覆盖 `color`/`borderColor` 以保持继承。
- Dark mode 通过 `darkMode: 'class'` 与 `next-themes` 协作，由运行时注入 `class="dark"` 而非媒体查询。