Next.js 16 (Export 模式) + React 18 + TypeScript 5 + Tailwind CSS 3 (`@tailwindcss/typography`)；Markdown 渲染使用 `marked` + `react-markdown` + `remark-gfm`，代码高亮用 `react-syntax-highlighter`，图标库为 `lucide-react`，UI 原子类组合借助 `class-variance-authority` + `clsx` + `tailwind-merge`。